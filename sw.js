const CACHE="bluegate-store-v5";const ASSETS=["./","./index.html","./css/store.css","./js/config.js","./js/api.js","./js/rates.js","./js/pricing.js","./js/checkout.js","./js/app.js","./assets/logo.png","./assets/logo-mark.png"];self.addEventListener("install",e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS))));self.addEventListener("activate",e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k))))));self.addEventListener("fetch",e=>{const u=new URL(e.request.url);if(u.hostname.includes("supabase.co")||u.hostname.includes("wallex")||u.hostname.includes("nobitex")||u.hostname.includes("binance")||u.hostname.includes("coingecko"))return;e.respondWith(fetch(e.request).then(r=>{const copy=r.clone();caches.open(CACHE).then(c=>c.put(e.request,copy));return r}).catch(()=>caches.match(e.request)))})